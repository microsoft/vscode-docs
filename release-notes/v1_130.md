---
Order: 140
TOCTitle: 1.130
PageTitle: Visual Studio Code 1.130
MetaDescription: Learn what's new in Visual Studio Code 1.130
MetaSocialImage: 1_130/release-highlights.webp
Date: 2026-07-22
DownloadVersion: 1.130.0
Milestone: 1.130.0
ProductEdition: Stable
---
# Visual Studio Code 1.130

Follow us on [LinkedIn](https://www.linkedin.com/showcase/vs-code), [X](https://go.microsoft.com/fwlink/?LinkID=533687), [Bluesky](https://bsky.app/profile/vscode.dev) <!-- %IF INSIDERS % | Follow Insiders Changelog on [X](https://x.com/VSCodeChangelog) or [Bluesky](https://bsky.app/profile/vscodechangelog.bsky.social) %ENDIF % --> <!-- %IF IN_PRODUCT % | [View online](https://code.visualstudio.com/updates)%ENDIF % -->

---

_Release date: July 22, 2026_

<!-- DOWNLOAD_LINKS_PLACEHOLDER -->

---

Welcome to the 1.130 release of Visual Studio Code. This release ... <TODO @ntrogh>

* [highlight](#bookmark): <highlight description>

Happy Coding!

---

<!-- %IF STABLE %
VS Code is rolling out gradually to all users. Use **Check for Updates** in VS Code to get the latest version immediately.

To try new features as soon as possible, [**download the nightly Insiders build**](https://code.visualstudio.com/insiders), which includes the latest updates as soon as they are available.

---
%ENDIF % -->

<!-- TOC
<div class="toc-nav-layout">
  <nav id="toc-nav">
    <div>In this update</div>
    <ul>
      <li><a href="#agents">Agents</a></li>
      <li><a href="#chat">Chat</a></li>
      <li><a href="#mcp">MCP</a></li>
      <li><a href="#accessibility">Accessibility</a></li>
      <li><a href="#editor-experience">Editor Experience</a></li>
      <li><a href="#code-editing">Code Editing</a></li>
      <li><a href="#notebooks">Notebooks</a></li>
      <li><a href="#source-control">Source Control</a></li>
      <li><a href="#debugging">Debugging</a></li>
      <li><a href="#tasks">Tasks</a></li>
      <li><a href="#terminal">Terminal</a></li>
      <li><a href="#authentication">Authentication</a></li>
      <li><a href="#languages">Languages</a></li>
      <li><a href="#remote-development">Remote Development</a></li>
      <li><a href="#contributions-to-extensions">Contributions to extensions</a></li>
      <li><a href="#extension-authoring">Extension Authoring</a></li>
      <li><a href="#proposed-apis">Proposed APIs</a></li>
      <li><a href="#engineering">Engineering</a></li>
      <li><a href="#deprecated-features-and-settings">Deprecated features and settings</a></li>
      <li><a href="#notable-fixes">Notable fixes</a></li>
      <li><a href="#thank-you">Thank you</a></li>
    </ul>
  </nav>
  <div class="notes-main">
Navigation End -->

## Agents

### The agent host

As mentioned in our last release, we're rearchitecting how agent sessions work in VS Code around the agent host - a dedicated process that runs agent harnesses such as Copilot, Claude, and Codex, based on the [Agent Host Protocol](https://microsoft.github.io/agent-host-protocol/) (AHP). Because a session lives in its own process, the same session can be connected to and rendered from multiple VS Code windows at once. The agent host's Copilot agent is powered by the [Copilot SDK](https://www.npmjs.com/package/@github/copilot-sdk), which means that its behavior and functionality is aligned with the Copilot CLI, the standalone GitHub Copilot app, and other Copilot products.

Learn more about the [VS Code Agent Host architecture](https://code.visualstudio.com/docs/agents/concepts/agent-host).

We're actively developing the agent host and progressively rolling it out to users in both the editor window and the [Agents window](https://code.visualstudio.com/docs/agents/agents-window). To opt in, enable `setting(chat.agentHost.enabled)` and then pick an agent host harness from the harness dropdown. The screenshot below shows how to select the `Copilot` harness on the agent host in the editor window:

![Screenshot showing the harness dropdown in the editor window.](images/1_130/agent-host-harness-dropdown-editor.webp)

As we continue to invest in the agent host, some features may only be available when an agent runs on it. Those features link back to this section and, where relevant, note any additional settings that enable them (for example, `setting(chat.agents.claude.preferAgentHost)` to enable the Claude agent on the agent host).

If you have any feedback or requests while using the agent host, please let us know by [filing an issue](https://github.com/microsoft/vscode/issues).

#### Assisted tool approvals

**Setting**: `setting(chat.assistedPermissions.enabled)`

Repeated tool approval prompts can interrupt long-running agent tasks. With assisted permissions, the language model evaluates the risk of each tool call and decides whether the tool can run or should require your approval.

Enable the setting to add **Assisted permissions** to the permissions picker for agents that run on the agent host. The following video compares default approvals with assisted permissions:

<video src="images/1_130/assisted-approvals-v-default.mp4" title="Video showing how assisted permissions evaluate tool calls compared with default approvals." autoplay loop controls muted></video>

### Agents window improvements (Preview)

The [Agents window](https://code.visualstudio.com/docs/agents/agents-window) includes several updates that make it easier to review changes and manage chats.

#### File-level diff statistics

Each file header in the **Changes** editor shows live insertion and deletion counts next to the file path. You can quickly assess the size of each file's changes while scanning a multi-file diff.

#### Compact multi-file diff view

The multi-file diff uses a more compact gutter that removes empty space before the code. File headers, line numbers, and unchanged-region controls share a consistent alignment, which leaves more room for reviewing changes in a narrow editor.

![Screenshot showing file-level diff statistics and compact, aligned gutters in the multi-file diff view.](images/1_130/agents-compact-diff.png)

#### Compact quick chats

Quick chats use compact, single-line rows in the sessions list. Regular sessions retain a second line with change statistics, status, and timestamps, making quick chats easier to distinguish and leaving more room for project sessions.

![Screenshot showing compact quick-chat rows in the sessions list.](images/1_130/agents-compact-quick-chat.png)

#### Worktree support for all agent harnesses

The **New Worktree** checkbox in the Agents window was previously only supported by the Copilot harness. Claude and Codex sessions also run in a Git worktree, making it easier to spin up parallel sessions for different features in the same workspace regardless of harness.

![Screenshot showing the New Worktree option for a Claude agent session.](images/1_130/agents-worktree.png)

## Chat

### Chat timestamps

**Setting**: `setting(chat.verbose)`

 Timestamps are now shown for rchat requests and responses. Hover over the message toolbar to view the timestamp and elapsed time for a chat interaction. You can disable this with `setting(chat.assistedPermissions.enabled)`

<video src="images/1_130/timestamps-in-chat.mov" title="Video showing timestamps and elapsed time in a chat message toolbar." autoplay loop controls muted></video>

### Aggregate AI credit usage for Copilot Business and Enterprise

Copilot Business and Copilot Enterprise users can now see their aggregate AI credit usage for the current billing cycle directly in the Copilot status menu. Previously, credit usage was only surfaced when a user-level budget was configured, leaving many organization-managed users without visibility into how many credits they had consumed.

Now, when no user-level budget is set, the status menu displays the total number of credits used so far in the billing cycle. This gives you an at-a-glance view of your consumption, so you can better understand your usage patterns without leaving the editor.

![Screenshot showing the Copilot status menu displaying aggregate credit usage for a Copilot Enterprise user.](images/1_130/aggregate-credit-usage.png)


## MCP


## Accessibility


## Editor Experience


## Code Editing


## Notebooks


## Source Control


## Debugging


## Tasks


## Terminal

### Clickable file links in Git diffs with mnemonic prefixes

You can open file links directly from Git diff output in the terminal when Git's [`diff.mnemonicPrefix`](https://git-scm.com/docs/diff-config#Documentation/diff-config.txt-diffmnemonicPrefix) option is enabled. VS Code recognizes prefixes such as `i/` for the index and `w/` for the working tree, and removes the prefix from the link target so the correct file opens.

When mnemonic prefixes are enabled, VS Code also recognizes the numeric `1/` and `2/` prefixes produced by `git diff --no-index`.


## Authentication


## Languages


## Remote Development

The [Remote Development extensions](https://marketplace.visualstudio.com/items?itemName=ms-vscode-remote.vscode-remote-extensionpack), allow you to use a [Dev Container](https://code.visualstudio.com/docs/devcontainers/containers), remote machine via SSH or [Remote Tunnels](https://code.visualstudio.com/docs/remote/tunnels), or the [Windows Subsystem for Linux](https://learn.microsoft.com/windows/wsl) (WSL) as a full-featured development environment.

Highlights include:

* TODO: @ntrogh

You can learn more about these features in the [Remote Development release notes](https://github.com/microsoft/vscode-docs/blob/main/remote-release-notes/v1_130.md).


## Contributions to extensions


## Extension Authoring


## Proposed APIs


## Engineering

The VS Code repository is now compiled using the release version of TypeScript 7. We also switched to the release version of the TypeScript 7 extension. The official release announcement from the TypeScript team can be found [here](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/).

## Deprecated features and settings

### New deprecations in this release

### Upcoming deprecations


## Notable fixes


## Thank you

Got it — I’ll locate the acknowledgments generation logic first, then use the same rules to produce the 1.130 contribution list.

Searched for `acknowledg|acknowledgement|acknowledgment`

Searched for `milestone`

Searched for `acknowledg|acknowledgement|acknowledgment`

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/components/views/acknowledgement/hooks.ts)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/components/views/acknowledgement/view.tsx)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/components/views/acknowledgement/reducer.ts)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/hooks/useQueryPullRequests.ts)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/hooks/useQueryAssignees.ts)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/components/common/RepositorySelector.tsx)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/client/src/hooks/useQueryRepositories.ts)

Searched for `RepositoryType\.GitHub|/api/repositories|class .*Repository`

Searched for `export interface IRepository|enum RepositoryType`

Read [](file:///Users/martinae/workspaces/vscode-tools/src/common/src/repositories.ts)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/server/src/repositories/repositories.ts)

Read [](file:///Users/martinae/workspaces/vscode-tools/src/server/src/issues.ts), lines 1 to 140

I found the exact filtering/sorting rules in the acknowledgements view. Next I’ll pull the repository set and apply those same rules for milestone `1.130`.

"Fetch Web Content" failed
Completed with input: {"url":"https://github.com/microsoft/vscode/wiki/Endgame-Template#friday"}

"Fetch Web Content" failed
Completed with input: {"url":"https://raw.githubusercontent.com/wiki/microsoft/vscode/Endgame-Template.md"}

Fetched [https://github.com/microsoft/vscode/wiki/](https://github.com/microsoft/vscode/wiki/)
Completed with input: https://github.com/microsoft/vscode/wiki/

Searched for `https://github.com/microsoft/`

Searched for `endgame.*repos|repositories.*endgame|repo list|vscode-.*repository`

Found files matching `**/*repositor*.json`

Searched for `vscode-repo|endgame template|endgame repositories|tracked endgame`

Read [](file:///Users/martinae/workspaces/vscode-tools/src/server/src/endgame/tpi.ts), lines 120 to 220
Completed with input: {
  "path": "/Users/martinae/workspaces/vscode-tools/src/server/src/endgame/tpi.ts",
  "view_range": [
    120,
    220
  ]
}

Read [](file:///Users/martinae/workspaces/vscode-tools/src/server/src/endgame/tpi.ts), lines 400 to 500
Completed with input: {
  "path": "/Users/martinae/workspaces/vscode-tools/src/server/src/endgame/tpi.ts",
  "view_range": [
    400,
    500
  ]
}

Read [](file:///Users/martinae/workspaces/vscode-tools/README.md), lines 1 to 260
Completed with input: {
  "path": "/Users/martinae/workspaces/vscode-tools/README.md",
  "view_range": [
    1,
    260
  ]
}

Found files matching `**/repositories*.test.ts`

Searched for `microsoft/vscode`

Searched for `repo:microsoft/`

Ran terminal command: curl -sS -D - https://tools.code.visualstudio.com/api/repositories?type=github -o /tmp/repos.json | head -n 40

Ran terminal command: gh auth status

Ran terminal command: gh issue list -R microsoft/vscode --label endgame-plan --search 'milestone:"1.130"' --state all --limit 20 --json number,title,milestone,state,closedAt,createdAt,url

Ran terminal command: gh issue list -R microsoft/vscode --search '1.130 endgame in:title' --state all --limit 20 --json number,title,state,url

Ran terminal command: gh search code 'Endgame Template Friday repo:microsoft/vscode' --limit 20 --json path,repository,url

Ran terminal command: tmpdir=$(mktemp -d); git clone --depth 1 https://github.com/microsoft/vscode.wiki.git "$tmpdir/wiki" >/dev/null 2>&1 && ls "$tmpdir/wiki" | head && echo "WIKI_DIR=$tmpdir/wiki"

Found files matching `**/*Endgame*Template*.md`

Found files matching `**/*endgame*template*.md`

Searched for `Endgame|endgame`

Read [](file:///var/folders/ks/dczqb8jd4dj4z_d4ngh7nbl40000gn/T/tmp.Q6nSDqK0KW/wiki/Running-the-Endgame.md)
Completed with input: {
  "path": "/var/folders/ks/dczqb8jd4dj4z_d4ngh7nbl40000gn/T/tmp.Q6nSDqK0KW/wiki/Running-the-Endgame.md"
}

Searched for `Friday|repo:microsoft|vscode-cli|vscode-cpptools|Acknowledge`

Ran terminal command: gh issue list -R microsoft/vscode --label endgame-plan --state all --limit 50 --json number,title,milestone,createdAt,updatedAt,state,url

Ran terminal command: gh issue view 326293 -R microsoft/vscode --json body,title,url

Ran terminal command: gh api --paginate /search/issues -f q='org:microsoft is:pr (milestone:"1.130.0" OR label:"1.130.0") (is:merged OR is:closed)' -f per_page=100 --jq '.items[] | [.repository_url,.number,.user.login,.state,.milestone.title] | @tsv' | head

Ran terminal command: gh api search/issues -f q='repo:microsoft/vscode is:pr milestone:"1.130.0" is:merged' -f per_page=5 --jq '.items[] | .number'

Ran terminal command: gh search prs --help | sed -n '1,140p'

Ran terminal command: gh search prs --owner microsoft --milestone '1.130.0' --merged --limit 5 --json number,title,url,author,state,repository,labels

Ran terminal command: python - <<'PY'
import subprocess, requests, json, sys, re
from collections import defaultdict

MILESTONE='1.130.0'
TOKEN=subprocess.check_output(['gh','auth','token'], text=True).strip()
headers={'Authorization':f'Bearer {TOKEN}','Accept':'application/vnd.github+json'}

# GraphQL helper
GQL_URL='https://api.github.com/graphql'
def gql(query, variables=None):
    r=requests.post(GQL_URL, headers=headers, json={'query':query,'variables':variables or {}}, timeout=60)
    if r.status_code!=200:
        raise RuntimeError(f'GraphQL {r.status_code}: {r.text[:400]}')
    data=r.json()
    if 'errors' in data:
        raise RuntimeError(f"GraphQL errors: {data['errors']}")
    return data['data']

# fetch vscode assignees (exclude internal contributors)
assignees=set()
cursor=None
query='''
query($cursor:String){
  repository(owner:"microsoft", name:"vscode") {
    assignableUsers(first:100, after:$cursor){
      nodes{login}
      pageInfo{hasNextPage endCursor}
    }
  }
}
'''
while True:
    d=gql(query, {'cursor':cursor})['repository']['assignableUsers']
    for n in d['nodes']:
        assignees.add(n['login'].lower())
    if not d['pageInfo']['hasNextPage']:
        break
    cursor=d['pageInfo']['endCursor']

# REST search helper
SEARCH_URL='https://api.github.com/search/issues'
def search_all(q):
    items=[]
    page=1
    while True:
        r=requests.get(SEARCH_URL, headers=headers, params={'q':q,'per_page':100,'page':page}, timeout=60)
        if r.status_code!=200:
            raise RuntimeError(f'Search {r.status_code} for {q}: {r.text[:300]}')
        d=r.json()
        batch=d.get('items',[])
        items.extend(batch)
        if len(batch)<100:
            break
        page += 1
        if page>10: # GitHub search API limit
            break
    return items

queries=[
    f'org:microsoft is:pr milestone:"{MILESTONE}" is:closed',
    f'org:microsoft is:pr label:"{MILESTONE}" is:closed',
]

all_items=[]
for q in queries:
    all_items.extend(search_all(q))

# dedupe and collect needed prs
by_url={}
need_merge_check=[]
for it in all_items:
    url=it['html_url']
    if url in by_url:
        # merge labels from duplicate hits
        existing_labels={l['name'] for l in by_url[url].get('labels',[])}
        for l in it.get('labels',[]):
            if l['name'] not in existing_labels:
                by_url[url]['labels'].append(l)
        continue
    by_url[url]=it
    need_merge_check.append(it)

# Determine merge state via GraphQL in batches
node_ids=[it['node_id'] for it in need_merge_check if it.get('node_id')]
merged_map={}
for i in range(0,len(node_ids),50):
    ids=node_ids[i:i+50]
    q='query($ids:[ID!]!){ nodes(ids:$ids){ ... on PullRequest { id merged mergedAt } }}'
    data=gql(q, {'ids':ids})['nodes']
    for n in data:
        if n:
            merged_map[n['id']]=bool(n['merged'])

repos=defaultdict(list)
for it in by_url.values():
    user=it.get('user') or {}
    login=user.get('login')
    if not login or user.get('type')!='User':
        continue
    if login.lower() in assignees:
        continue

    labels=[l['name'] for l in it.get('labels',[])]
    milestone_obj=it.get('milestone')
    milestone_title=milestone_obj['title'] if milestone_obj else None
    is_merged=merged_map.get(it.get('node_id'), False)

    include=False
    if milestone_title == MILESTONE:
        if is_merged:
            include=True
        else:
            include='on-release-notes' in labels
    else:
        include=MILESTONE in labels

    if not include:
        continue

    repo=it['repository_url'].rstrip('/').split('/')[-1]
    repos[repo].append({
        'login': login,
        'author_url': user.get('html_url') or f'https://github.com/{login}',
        'title': it['title'],
        'number': it['number'],
        'url': it['html_url'],
    })

# fetch names for contributors
logins=sorted({pr['login'] for prs in repos.values() for pr in prs})
name_map={}
for i in range(0,len(logins),40):
    batch=logins[i:i+40]
    alias_parts=[]
    for idx,login in enumerate(batch):
        alias=f'u{idx}'
        alias_parts.append(f'{alias}: user(login: "{login}") {{ login name url }}')
    q='query { ' + ' '.join(alias_parts) + ' }'
    data=gql(q)
    for v in data.values():
        if v and v.get('login'):
            name_map[v['login']]=v.get('name')

# format exactly like tool
ordered_repos=sorted(repos.keys(), key=lambda r: (0 if r.startswith('vscode') else 1, r.lower()))
out=[]
for repo in ordered_repos:
    prs=sorted(repos[repo], key=lambda p: p['login'].lower())
    if not prs:
        continue
    out.append(f'Contributions to `{repo}`:')
    out.append('')

    busy=set()
    for i in range(len(prs)-1):
        if prs[i]['login'].lower()==prs[i+1]['login'].lower():
            busy.add(prs[i]['login'])

    current_busy=''
    for pr in prs:
        login=pr['login']
        name=name_map.get(login)
        author=f'[@{login} ({name})]({pr["author_url"]})' if name else f'[@{login}]({pr["author_url"]})'
        prtext=f'{pr["title"]} [PR #{pr["number"]}]({pr["url"]})'
        if login in busy:
            if login!=current_busy:
                out.append(f'* {author}')
                current_busy=login
            out.append(f'  * {prtext}')
        else:
            out.append(f'* {author}: {prtext}')
    out.append('')

print('\n'.join(out))
PY

Ran terminal command: python3 - <<'PY'
import subprocess, requests
from collections import defaultdict

MILESTONE='1.130.0'
TOKEN=subprocess.check_output(['gh','auth','token'], text=True).strip()
headers={'Authorization':f'Bearer {TOKEN}','Accept':'application/vnd.github+json'}

GQL_URL='https://api.github.com/graphql'
def gql(query, variables=None):
    r=requests.post(GQL_URL, headers=headers, json={'query':query,'variables':variables or {}}, timeout=60)
    if r.status_code!=200:
        raise RuntimeError(f'GraphQL {r.status_code}: {r.text[:400]}')
    data=r.json()
    if 'errors' in data:
        raise RuntimeError(f"GraphQL errors: {data['errors']}")
    return data['data']

assignees=set()
cursor=None
query='''
query($cursor:String){
  repository(owner:"microsoft", name:"vscode") {
    assignableUsers(first:100, after:$cursor){
      nodes{login}
      pageInfo{hasNextPage endCursor}
    }
  }
}
'''
while True:
    d=gql(query, {'cursor':cursor})['repository']['assignableUsers']
    assignees.update(n['login'].lower() for n in d['nodes'])
    if not d['pageInfo']['hasNextPage']:
        break
    cursor=d['pageInfo']['endCursor']

SEARCH_URL='https://api.github.com/search/issues'
def search_all(q):
    items=[]
    for page in range(1,11):
        r=requests.get(SEARCH_URL, headers=headers, params={'q':q,'per_page':100,'page':page}, timeout=60)
        if r.status_code!=200:
            raise RuntimeError(f'Search {r.status_code} for {q}: {r.text[:300]}')
        batch=r.json().get('items',[])
        items.extend(batch)
        if len(batch)<100:
            break
    return items

queries=[
    f'org:microsoft is:pr milestone:"{MILESTONE}" is:closed',
    f'org:microsoft is:pr label:"{MILESTONE}" is:closed',
]

by_url={}
for q in queries:
    for it in search_all(q):
        u=it['html_url']
        if u not in by_url:
            by_url[u]=it
        else:
            seen={l['name'] for l in by_url[u].get('labels',[])}
            for l in it.get('labels',[]):
                if l['name'] not in seen:
                    by_url[u]['labels'].append(l)

node_ids=[it['node_id'] for it in by_url.values() if it.get('node_id')]
merged_map={}
for i in range(0,len(node_ids),50):
    ids=node_ids[i:i+50]
    q='query($ids:[ID!]!){ nodes(ids:$ids){ ... on PullRequest { id merged } }}'
    for n in gql(q, {'ids':ids})['nodes']:
        if n:
            merged_map[n['id']]=bool(n['merged'])

repos=defaultdict(list)
for it in by_url.values():
    user=it.get('user') or {}
    login=user.get('login')
    if not login or user.get('type')!='User':
        continue
    if login.lower() in assignees:
        continue

    labels=[l['name'] for l in it.get('labels',[])]
    milestone_obj=it.get('milestone')
    milestone_title=milestone_obj['title'] if milestone_obj else None
    is_merged=merged_map.get(it.get('node_id'), False)

    if milestone_title == MILESTONE:
        include=is_merged or ('on-release-notes' in labels)
    else:
        include=MILESTONE in labels

    if not include:
        continue

    repo=it['repository_url'].rstrip('/').split('/')[-1]
    repos[repo].append({
        'login': login,
        'author_url': user.get('html_url') or f'https://github.com/{login}',
        'title': it['title'],
        'number': it['number'],
        'url': it['html_url'],
    })

logins=sorted({pr['login'] for prs in repos.values() for pr in prs})
name_map={}
for i in range(0,len(logins),40):
    batch=logins[i:i+40]
    fields=' '.join(f'u{idx}: user(login: "{login}") {{ login name }}' for idx,login in enumerate(batch))
    for v in gql('query { '+fields+' }').values():
        if v and v.get('login'):
            name_map[v['login']]=v.get('name')

ordered_repos=sorted(repos.keys(), key=lambda r: (0 if r.startswith('vscode') else 1, r.lower()))
out=[]
for repo in ordered_repos:
    prs=sorted(repos[repo], key=lambda p: p['login'].lower())
    if not prs:
        continue
    out.append(f'Contributions to `{repo}`:')
    out.append('')

    busy=set()
    for i in range(len(prs)-1):
        if prs[i]['login'].lower()==prs[i+1]['login'].lower():
            busy.add(prs[i]['login'])

    current=''
    for pr in prs:
        login=pr['login']
        nm=name_map.get(login)
        author=f'[@{login} ({nm})](https://github.com/{login})' if nm else f'[@{login}](https://github.com/{login})'
        prtext=f"{pr['title']} [PR #{pr['number']}]({pr['url']})"
        if login in busy:
            if login!=current:
                out.append(f'* {author}')
                current=login
            out.append(f'  * {prtext}')
        else:
            out.append(f'* {author}: {prtext}')
    out.append('')

print('\n'.join(out))
PY

Ran terminal command: python3 - <<'PY'
import subprocess, json, urllib.request, urllib.parse
from collections import defaultdict

MILESTONE='1.130.0'
TOKEN=subprocess.check_output(['gh','auth','token'], text=True).strip()


def http_json(url, method='GET', data=None):
    headers={
        'Authorization':f'Bearer {TOKEN}',
        'Accept':'application/vnd.github+json',
        'User-Agent':'vscode-tools-ack-script'
    }
    body=None
    if data is not None:
        body=json.dumps(data).encode('utf-8')
        headers['Content-Type']='application/json'
    req=urllib.request.Request(url, data=body, headers=headers, method=method)
    with urllib.request.urlopen(req, timeout=60) as r:
        return json.loads(r.read().decode('utf-8'))

def gql(query, variables=None):
    d=http_json('https://api.github.com/graphql', method='POST', data={'query':query,'variables':variables or {}})
    if 'errors' in d:
        raise RuntimeError(str(d['errors']))
    return d['data']

# assignees from microsoft/vscode
assignees=set()
cursor=None
query='''
query($cursor:String){
  repository(owner:"microsoft", name:"vscode") {
    assignableUsers(first:100, after:$cursor){
      nodes{login}
      pageInfo{hasNextPage endCursor}
    }
  }
}
'''
while True:
    d=gql(query, {'cursor':cursor})['repository']['assignableUsers']
    assignees.update(n['login'].lower() for n in d['nodes'])
    if not d['pageInfo']['hasNextPage']:
        break
    cursor=d['pageInfo']['endCursor']

def search_all(q):
    items=[]
    for page in range(1,11):
        params=urllib.parse.urlencode({'q':q,'per_page':100,'page':page})
        d=http_json('https://api.github.com/search/issues?'+params)
        batch=d.get('items',[])
        items.extend(batch)
        if len(batch)<100:
            break
    return items

queries=[
    f'org:microsoft is:pr milestone:"{MILESTONE}" is:closed',
    f'org:microsoft is:pr label:"{MILESTONE}" is:closed',
]

by_url={}
for q in queries:
    for it in search_all(q):
        u=it['html_url']
        if u not in by_url:
            by_url[u]=it
        else:
            seen={l['name'] for l in by_url[u].get('labels',[])}
            for l in it.get('labels',[]):
                if l['name'] not in seen:
                    by_url[u]['labels'].append(l)

node_ids=[it['node_id'] for it in by_url.values() if it.get('node_id')]
merged_map={}
for i in range(0,len(node_ids),50):
    ids=node_ids[i:i+50]
    q='query($ids:[ID!]!){ nodes(ids:$ids){ ... on PullRequest { id merged } }}'
    for n in gql(q, {'ids':ids})['nodes']:
        if n:
            merged_map[n['id']]=bool(n['merged'])

repos=defaultdict(list)
for it in by_url.values():
    user=it.get('user') or {}
    login=user.get('login')
    if not login or user.get('type')!='User':
        continue
    if login.lower() in assignees:
        continue

    labels=[l['name'] for l in it.get('labels',[])]
    milestone_obj=it.get('milestone')
    milestone_title=milestone_obj['title'] if milestone_obj else None
    is_merged=merged_map.get(it.get('node_id'), False)

    if milestone_title == MILESTONE:
        include=is_merged or ('on-release-notes' in labels)
    else:
        include=MILESTONE in labels

    if not include:
        continue

    repo=it['repository_url'].rstrip('/').split('/')[-1]
    repos[repo].append({
        'login': login,
        'title': it['title'],
        'number': it['number'],
        'url': it['html_url'],
    })

# fetch names
logins=sorted({pr['login'] for prs in repos.values() for pr in prs})
name_map={}
for i in range(0,len(logins),40):
    batch=logins[i:i+40]
    fields=' '.join(f'u{idx}: user(login: "{login}") {{ login name }}' for idx,login in enumerate(batch))
    for v in gql('query { '+fields+' }').values():
        if v and v.get('login'):
            name_map[v['login']]=v.get('name')

ordered_repos=sorted(repos.keys(), key=lambda r: (0 if r.startswith('vscode') else 1, r.lower()))
out=[]
for repo in ordered_repos:
    prs=sorted(repos[repo], key=lambda p: p['login'].lower())
    if not prs:
        continue
    out.append(f'Contributions to `{repo}`:')
    out.append('')

    busy=set()
    for i in range(len(prs)-1):
        if prs[i]['login'].lower()==prs[i+1]['login'].lower():
            busy.add(prs[i]['login'])

    current=''
    for pr in prs:
        login=pr['login']
        nm=name_map.get(login)
        author=f'[@{login} ({nm})](https://github.com/{login})' if nm else f'[@{login}](https://github.com/{login})'
        prtext=f"{pr['title']} [PR #{pr['number']}]({pr['url']})"
        if login in busy:
            if login!=current:
                out.append(f'* {author}')
                current=login
            out.append(f'  * {prtext}')
        else:
            out.append(f'* {author}: {prtext}')
    out.append('')

print('\n'.join(out))
PY

Ran terminal command: jq --version

Ran terminal command: gh search prs --owner microsoft --milestone '1.130.0' --state closed --limit 1 --json id,url | jq .

Ran terminal command: python3 - <<'PY'
import subprocess, json
from collections import defaultdict

MILESTONE='1.130.0'

def gh_json(args):
    out=subprocess.check_output(['gh', *args], text=True)
    return json.loads(out)

# 1) assignees from microsoft/vscode
assignees=gh_json(['api','--paginate','/repos/microsoft/vscode/assignees?per_page=100'])
assignee_logins={a['login'].lower() for a in assignees if a.get('login')}

# 2) candidate PR node IDs from milestone + label search
fields='id,url'
milestone_hits=gh_json(['search','prs','--owner','microsoft','--milestone',MILESTONE,'--state','closed','--limit','1000','--json',fields])
label_hits=gh_json(['search','prs','--owner','microsoft','--label',MILESTONE,'--state','closed','--limit','1000','--json',fields])
node_ids=[]
seen=set()
for item in milestone_hits+label_hits:
    nid=item.get('id')
    if nid and nid not in seen:
        seen.add(nid)
        node_ids.append(nid)

# 3) pull full data by GraphQL chunk
prs=[]
for i in range(0,len(node_ids),50):
    batch=node_ids[i:i+50]
    ids_json=json.dumps(batch)
    q=f'''
query {{
  nodes(ids: {ids_json}) {{
    ... on PullRequest {{
      id
      number
      title
      url
      merged
      milestone {{ title }}
      repository {{ name }}
      author {{
        ... on User {{
          login
          name
          url
        }}
      }}
      labels(first: 50) {{
        nodes {{ name }}
      }}
    }}
  }}
}}
'''
    data=gh_json(['api','graphql','-f',f'query={q}'])
    prs.extend([n for n in data['data']['nodes'] if n])

# 4) filter and group
repos=defaultdict(list)
for pr in prs:
    author=pr.get('author')
    if not author or not author.get('login'):
        continue
    login=author['login']
    if login.lower() in assignee_logins:
        continue

    labels=[n['name'] for n in (pr.get('labels') or {}).get('nodes',[]) if n and n.get('name')]
    milestone_title=((pr.get('milestone') or {}).get('title'))

    if milestone_title == MILESTONE:
        if (not pr.get('merged')) and ('on-release-notes' not in labels):
            continue
    else:
        if MILESTONE not in labels:
            continue

    repos[pr['repository']['name']].append(pr)

# 5) format like tool
out=[]
for repo in sorted(repos.keys(), key=lambda r: (0 if r.startswith('vscode') else 1, r.lower())):
    prs_sorted=sorted(repos[repo], key=lambda p: p['author']['login'].lower())
    if not prs_sorted:
        continue
    out.append(f'Contributions to `{repo}`:')
    out.append('')

    busy=set()
    for idx in range(len(prs_sorted)-1):
        if prs_sorted[idx]['author']['login'] == prs_sorted[idx+1]['author']['login']:
            busy.add(prs_sorted[idx]['author']['login'])

    current=''
    for pr in prs_sorted:
        a=pr['author']
        if a.get('name'):
            contributor=f"[@{a['login']} ({a['name']})]({a['url']})"
        else:
            contributor=f"[@{a['login']}]({a['url']})"
        prline=f"{pr['title']} [PR #{pr['number']}]({pr['url']})"

        if a['login'] in busy:
            if a['login'] != current:
                out.append(f'* {contributor}')
                current=a['login']
            out.append(f'  * {prline}')
        else:
            out.append(f'* {contributor}: {prline}')

    out.append('')

print('\n'.join(out))
PY

Yep — using the exact filters from the acknowledgements logic in [hooks.ts](/Users/martinae/workspaces/vscode-tools/src/client/src/components/views/acknowledgement/hooks.ts), here is the generated list for milestone **1.130** (milestone title **1.130.0**):

Contributions to `vscode`:

* [@accnops (Arthur Cnops)](https://github.com/accnops)
  * voice: NACK + client revalidation for dropped narration [PR #325966](https://github.com/microsoft/vscode/pull/325966)
  * Voice: opt out of backend auto-narration (auto_narrate: false) [PR #325799](https://github.com/microsoft/vscode/pull/325799)
* [@ahmadawais (Ahmad Awais)](https://github.com/ahmadawais): Detect Command Code as an agent CLI for terminal tab titles [PR #324417](https://github.com/microsoft/vscode/pull/324417)
* [@AntonioLujanoLuna (Antonio Lujano Luna)](https://github.com/AntonioLujanoLuna): Fix BYOK Anthropic endpoints sending PDFs as image blocks [PR #324960](https://github.com/microsoft/vscode/pull/324960)
* [@arham766 (Shahrier Islam Arham)](https://github.com/arham766): chore: bump windows-process-tree to 0.8.0 to fix UTF-8 command lines in Process Explorer [PR #324283](https://github.com/microsoft/vscode/pull/324283)
* [@clintharrison (Clint Harrison)](https://github.com/clintharrison): Support mnemonic prefixes in git diff parsing for terminal links [PR #298490](https://github.com/microsoft/vscode/pull/298490)
* [@justjavac (迷渡)](https://github.com/justjavac): Decorations: fall back to lower-priority colors [PR #325422](https://github.com/microsoft/vscode/pull/325422)
* [@mirimadahmed (Mir)](https://github.com/mirimadahmed)
  * Voice agent always on streaming mode to support barge in [PR #326165](https://github.com/microsoft/vscode/pull/326165)
  * Fix voice barge-in protocol [PR #326159](https://github.com/microsoft/vscode/pull/326159)
  * Add scoped live Voice transcripts [PR #326134](https://github.com/microsoft/vscode/pull/326134)
  * Voice agent send voice language locale from the client [PR #325931](https://github.com/microsoft/vscode/pull/325931)
* [@pony-maggie (Lucas Ma)](https://github.com/pony-maggie): Avoid stale simple dialog folder updates [PR #321357](https://github.com/microsoft/vscode/pull/321357)
* [@smorimoto (Sora Morimoto)](https://github.com/smorimoto): Recognise OCaml in settings labels [PR #325457](https://github.com/microsoft/vscode/pull/325457)
* [@UditDewan (udit)](https://github.com/UditDewan): Fix tunnelProtocol context key always resolving to https on focus [PR #325445](https://github.com/microsoft/vscode/pull/325445)

### Issue tracking

Contributions to our issue tracking:

* [@gjsjohnmurray (John Murray)](https://github.com/gjsjohnmurray)
* [@RedCMD (RedCMD)](https://github.com/RedCMD)
* [@IllusionMH (Andrii Dieiev)](https://github.com/IllusionMH)
* [@albertosantini (Alberto Santini)](https://github.com/albertosantini)

---

We really appreciate people trying our new features as soon as they are ready, so check back here often and learn what's new.

>If you'd like to read release notes for previous VS Code versions, go to [Updates](https://code.visualstudio.com/updates) on [code.visualstudio.com](https://code.visualstudio.com).

<a id="scroll-to-top" role="button" title="Scroll to top" aria-label="scroll to top" href="#"><span class="icon"></span></a>
<link rel="stylesheet" type="text/css" href="css/inproduct_releasenotes.css"/>
