trigger:
  branches:
    include:
      - main
pr: none

resources:
  repositories:
    - repository: website
      type: github
      endpoint: Monaco
      name: microsoft/vscode-website
      ref: main # switch to prod when merged
    - repository: docs
      type: github
      endpoint: Monaco
      name: microsoft/vscode-docs
      ref: main

variables:
- template: .pipelines/templates/onebranch/variables.yml@website

extends:
  template: .pipelines/templates/onebranch/official.yml@website